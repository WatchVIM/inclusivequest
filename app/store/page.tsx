export default function Store(){
  return (
    <div className="pt-6">
      <h1 className="text-2xl font-black">Store</h1>
      <p className="mt-2 text-muted">
        Since videos are YouTube embeds, “purchase” is best positioned as paying for: curated access, premium accessibility features, and unlocking ASL avatar tracks.
      </p>

      <div className="mt-6 rounded-[22px] border border-line bg-card p-6 text-sm text-muted">
        Next step (optional): add user accounts + payments to unlock ASL tracks.
      </div>
    </div>
  );
}
